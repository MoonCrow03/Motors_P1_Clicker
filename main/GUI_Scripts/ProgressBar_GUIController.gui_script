function init(self)
	local clickText = gui.get_node("clicksText")
	gui.set_text(clickText, "(Not Working) 0 / 10")
end

function final(self)
	
end

function update(self, dt)
	
end

function on_message(self, message_id, message, sender)
	
end

